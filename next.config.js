/** @type {import('next').NextConfig} */
const nextConfig = {
  // Desactivar StrictMode evita el doble montaje que rompe Leaflet en desarrollo
  reactStrictMode: false,
}
module.exports = nextConfig
